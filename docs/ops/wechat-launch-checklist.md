# 微信服务号上线配置清单（manager 汇总 2026-09-14）

> 用途：服务号认证通过前后，所有需要人工去平台配置的事项总表。代码侧（Task 9/10）不含在内。
> 状态标记：[ ] 待办 / [x] 完成。认证预计 2026-09-15 通过。

## A. 现在就能做（不依赖认证）

- [x] **开发者平台 IP 白名单**：已配 `120.77.11.137`（2026-09-14 完成）。
- [ ] **被关注自动回复**：mp → 自动回复 → 被关注回复，文案：
      「感谢关注栗橙科技官方服务号！我们提供 AI 智能体与云工作区等在线软件服务。官网：scheng.net，点击即可访问体验；问题咨询请邮件 support@scheng.net。」
- [ ] **关键词自动回复（可选）**：关键词「100」「服务包」→ 回复充值链接文本。
- [ ] **商户平台关联服务号 AppID（硬前置，可先试）**：pay.weixin.qq.com → 产品中心 → AppID 账号管理 → 关联 `wxd0fbdccc072a49e2`（服务号管理员扫码确认）。若平台要求认证完成，则移到 B 组。
- [x] **支付宝收尾验证单**（2026-09-15 通过：出码+支付+到账全绿，应用公私钥配对与支付宝公钥均确认正确）。
- [ ] **微信收款限制申诉**：等结果（1-3 工作日）；驳回把原文发 manager。

## B. 认证通过后立刻做

- [ ] **关联验证域名（决策 2026-09-15：采 mp 功能设置「JS接口安全域名」对话框为唯一可用入口，5 域名槽/月 5 次修改，用 1 留 4；理由：全平台无第二入口+文件机制同源+新平台疑似合并名单；**同日追加：等认证通过后再动手，认证前保存必败白烧次数**）**：前置→认证若已通过先花 30s 复查有无新冒出的「网页授权域名」行（有则优先正主）；manager 先放 `MP_verify_BikG1GNzuH6axRKI.txt` 于机A站根并 curl 自证；域名1 填裸域 `scheng.net` 保存；随后重发菜单观察；OAuth 真验证=Task 9 部署后实测，若报 10003 再找独立授权域名入口/工单。
- [ ] **自定义菜单**（终版结构见下；**首次发布被拒：「链接内容不属于当前公众号」= 外链域名未进关联名单，等网页授权域名配好后重发**；名称卡≤4汉字）：
      - 「AI 服务」→ 跳转网页 `https://scheng.net/`
      - 「充值套餐」（父菜单）子菜单：「充值额度」→ `https://scheng.net/wallet`；「订阅套餐」→ `https://scheng.net/subscriptions`
      - 「联系客服」→ **发送消息→文字**：「客服邮箱 support@scheng.net；或访问官网 scheng.net 点击右下角客服按钮。」（kfid 外链永远“不属于本公众号”，不进菜单；改放关键词自动回复：用户发「客服」→ 回复带 `https://work.weixin.qq.com/kfid/kfc9a0826599eedd605` 的文本）
      - deeplink 小任务上线后：「充值额度」改 `https://scheng.net/wallet?amount=100`，可加「300元服务包」→ `?amount=300`
- [ ] **商户平台支付授权目录**：pay.weixin.qq.com → 产品中心 → JSAPI 支付 → 支付授权目录 → 加 `https://scheng.net/wallet/` 与 `https://scheng.net/subscriptions/`（**目录必须精确到调起支付页面所在目录、带尾斜杠**；旧文档里的 /console/ 是经典版路由，作废）（A 组关联完成后才可配）。
- [ ] **关联新 AppID 时严禁解绑老 AppID（wx5757dd64f1823d38）关联**——Native 扫码付仍依赖它，是「加」不是「换」。

## C. 明确不做（别碰）

- 消息推送：**不启用**（启用会顶掉自动回复/菜单，且我们无接收端点）。
- JS 接口安全域名：不配（WeixinJSBridge 直调不吃 JS-SDK config）。
- 主页商品展示：不配（微信小店实体商品位，虚拟 SaaS 不适用）。
- 开发者平台 AppSecret「重置/冻结」：不点（已生成的 secret 在凭据清单，重置即失效）。
- 商户平台退款配置：留空（无官方退款 API）。
- 服务号对话内收款码/转账：禁止（合规+对账红线）。

## D. 代码部署后（manager 执行重启，用户填值）

- [ ] Task 9 部署后：new-api 后台 → 支付网关 → 填 `WechatMpAppId` = `wxd0fbdccc072a49e2`、`WechatMpAppSecret` = 凭据清单所存 → 保存 → manager `docker restart new-api`。
- [ ] Task 10 部署后：无新增配置项。
- [ ] deeplink 小任务部署后：菜单 amount 深链生效，回 B 组补菜单参数。

## E. 终验

- [ ] 微信内打开服务号菜单 → 静默登录 → 充值页 → JSAPI 收银台弹起 → ¥1 支付 → 额度到账。
- [ ] 桌面扫码登录两分支（已绑直登 / 未绑创建新账户）各走一遍。

## F. 日历与尾巴（不挡上线）

- [ ] **服务号年审**：认证有效期一年（约 2027-09 到期，300/年）——设日历提醒，过期掉认证=菜单外链/网页授权全灭。
- [ ] **AppSecret 轮换**：上线稳定后开发者平台重置一次 + 后台同步 + restart（该 secret 曾在对话明文出现）。
- [ ] **unionid 打通**（小程序上线后）：服务号+小程序同绑微信开放平台，回填 `wechat_accounts.unionid`。费用注记：开放平台开发者资质认证 300 元一次性（现行政策无年审）；社区口径绑定不受认证影响→先直接绑，被拦再付。
- [ ] **模板消息**：支付成功/额度到账服务通知，需申请模板（债台账）。
- [ ] **监控**：开发者平台接口监控加告警（授权/支付失败率）；机B 日志盯 wechat notify 非 200。

## 附：关键值速查

| 项 | 值 |
|---|---|
| 服务号 AppID | wxd0fbdccc072a49e2（gh_5d09d0d431e5） |
| 服务号 AppSecret | 见 e:\savvy-keys-inventory.md（勿外传） |
| Native 老 AppID | wx5757dd64f1823d38（不动） |
| 商户号 | 1000524187 |
| 机B 出口 IP | 120.77.11.137 |
| 网页授权域名 | scheng.net |
| 支付授权目录 | https://scheng.net/wallet/ 与 https://scheng.net/subscriptions/ |
